---
interface Props {
  tags: string[];
}
const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <section class="partner-tags" aria-label="Partners">
      <h3>Partners</h3>
      <ul>
        {tags.map((tag) => (
          <li>{tag}</li>
        ))}
      </ul>
    </section>
  )
}

<style>
  .partner-tags {
    margin-block: var(--space-4);
  }

  h3 {
    font-size: 1.1rem;
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    max-width: none;
  }

  /* Shares chip metrics with LinkList for one coherent chip family */
  li {
    margin: 0;
    font-size: 0.85rem;
    font-weight: 550;
    color: var(--ink-soft);
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: 999px;
    padding: 0.4rem 1rem;
  }
</style>
